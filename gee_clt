(ee) C:\Users\adwhy>earthengine upload table -h

## UPLOAD SINGLE CSV AS SPATIAL: 
>earthengine upload table --asset_id users/hywong/occ_gcloud/CIIN --x_column decimalLongitude --y_column decimalLatitude --crs EPSG:4326 gs://nymphs_bucket_occ/CIIN_2000_2020_TheWorld.csv
earthengine task list -l

## FOLDER UPLOAD: 
## GOOGLE SKD:
C:\Program Files (x86)\Google\Cloud SDK> gsutil config
gcloud auth login
gsutil ls gs://nymphs_bucket_occ
gcloud config set project PROJECT_ID

(ee) C:\Users\adwhy> earthengine upload table --asset_id=projects/nymphs/assets/occ_gcloud --x_column=decimalLongitude --y_column=decimalLatitude --crs=EPSG:4326 gs://nymphs_bucket_occ

## WHILE LOOP IN GIT BASH CONDA: 
gsutil ls gs://nymphs_bucket_occ/occdata_rmFull/*.csv | sed 's:.*/::' | cut -d '.' -f 1 > occName_list
gsutil ls gs://nymphs_bucket_occ/occdata_rmFull/*.csv > occ_list

cat occ_list | head -n 2 | tail -1 

## CONDA IN GIT BASH: 
C:\ProgramData\Anaconda3\etc\profile.d
. /c/ProgramData/Anaconda3/etc/profile.d/conda.sh

## TASKS 12122022
X. TEST PA POINTS UNIQNESS USING UNIQ-RASTER FILE
X. REDUCE PA POINTS TO EQUAL TO OCC

X. DOWNLOAD OCC GLOBALLY FOR GEE; USE REGISTERED DATASET FOR GBIF CITATION
X. SHELL IMPORT TO GEE ASSETS
X. TRANSLATE TO PYTHON API FOR REMAINING GEE CODE AND RUN

X. FIX PA-RELATED FUNC IN R AND TEST RUN A PLANT W/ EVALUATE AND FULL
x. TEST RUN 10 PLANTS AND CHECK ACCURACY MAKE SEE IF AUCS > 0.75  
	.. NOTE THAT MAXENT SHOULD BE REQUIRING A LARGE NUMBER OF BACKGROUND POINTS, COMBINE RESULTS ARE NEEDED? 

X. TEST UPPERBOUND OCC GEE CAN TAKE AND REDUCE OCC
	X.. REDUCED IN R IN A WAY THAT BALANCE THE OCC IN FOUR REGIONS OF WORLDS, AND REDUCED TO THE OCC UPPERBOUND IN GEE
	X.. 900 meter spatial reso with 4900 occ works, and GEE will output segments of plots
. DUE TO LACK OF OCC (E.G. LAAN81), MAY NEED TO RESAMPLE SOME OCC TO SEE IF CAN INCREASE? 

. TEST OTHER INDIVIDUAL MODELS ON GEE AND PUT ON RUN FOR ALL GLOBAL

. STANDARDIZE TO N(0,1) MAY BE NEEDED, TO RUN ANOTHER SET OF RESULTS WITH STANDARDIZED INPUT FROM GEE 
	.. STANDARDIZATION RESULTS IN COMP ERROR

#### BACKUPS
. IJGI 2023 TRAVEL AWARDS - RECOMMENDATION LETER






